#include <iostream>
using namespace std;

//return value of comparison: true(1) or false (0)

int x = 5;
int y = 3;

int main(){
    //cout << (x > y);
    cout << (x == y);
}

/* == : equal to
   != : not equal
   >  : greater than
   <  : less than
   >= : greater than or equal to
   <= : less than or equal to */